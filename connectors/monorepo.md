# Monorepo connector (optional)

When this package lives under `grokbuild/`:

| Sibling | Role |
|---------|------|
| `olivia-repo-template/` | Source template |
| `inventory-ci/` | Index auto-refresh |
| `persona-work/` | Host / persona SSoT |
| `CANON-TAG-*.md` | Workspace CANON tags |

**Standalone:** ignore this file. Nothing required.
