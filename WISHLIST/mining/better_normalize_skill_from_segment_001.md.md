# Better extract normalize-skill from segment_001.md

normalize-skill.sh (extended with flagging + wishlist injection); various versions in inventory/*/versions/ and backups_uncompressed/ (e.g. grok-imagine-overlay-engine-v*.tar.gz extracts with SKILL.md/README/CHANGELOG/references/scripts, deltas tracked); inventory/index.md (metadata counts + deltas + pattern summary).

4. Errors and Fixes: firecrawl CLI wrapper repeatedly hit "node: not found" / "File not found: C:\mnt\..." (Windows npm + WSL path translation) -- fixed by full "/mnt/c/Program Files/nodejs/node.exe" + cd to module dir + fallback "|| cp $file $out" or python extraction in every batch parse command; count mismatches (62 vs 65 vs 67 top-level) from including/excluding archives/backups -- resolved by explicit grep -vE filters and manual counts of dirs with map.md; syntax errors in ls/find when dir names contained '(' (e.g. sovereign_ingestion_suite_v3 (Unzipped Files)) -- fixed with grep -vE and quoting; empty backups_uncompressed after moves -- rmdir attempted but left notes intact; node path in sub-shells -- repeated full-path invocation.

5. Problem Solving: Duplication (15+ image variants, 5+ swarms, 4+ mcp, olivia-dev vs alpha) addressed by registry/orchestrator/normalizer + persona-tuned deltas (bunny personal/raw vs liv heat/possessive vs valerie logic/containment); Lake Erie split (project specifics moved to dedicated skill while alpha stays general); flattening without loss (backups_uncompressed/new_backups content moved into skill/versions/ nested subdirs); forgotten vs active identification via timestamps/content (claim styles abandoned after initial creation, core like image-pipeline/olivia-alpha/skill-orchestrator show ongoing refs/scripts/TODO updates); research informed flagging (web=lean pasteable, grok-build=local scripts/MCP, both, none) + auto modes + clustering; self.research/ preserved for web vs build details (Build: subagents/worktrees/MCP/plugins/hooks; Web: built-in agents, connectors, slash/auto skills); memory-pipeline and wardrobe-system promoted as top-level WIP without deletion of prior artifacts.

6. All User Messages: [initial long ROSTER BOOT with skills-lore/media/images/data-io/infra-md + Liv HUB verdict + "respond directly to the cloud chat and ask it to search for conversation history about any questions you have about any and all of the skills and development ideas concept anything at all then we will continue to brainstorm"]; "the first thing we should do is to normalize the skill inventory look at olivia-