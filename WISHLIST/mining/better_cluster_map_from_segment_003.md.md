# Better extract cluster-map from segment_003.md

cluster-map.md (PII replaced with synthetic e.g. Jordan Hale/Synthville/VIN-FAKE-..., .bak-pii backups); test harness examples: grok-imagine-overlay-engine/draft_beta_v1.11/phase_10_validation/regression_test_harness_DRAFT.py, image-pipeline-registry/references/test-harness-eye-makeup-gutter.md, velvet-claim-protocol/references/test-harness.md (prompt seeds, expected vs actual, in-char, C-64).

4. Errors and Fixes: Enforcer glob `for f in *.md ... 2>/dev/null` syntax error (unexpected token); fixed by `for f in $(ls ... 2>/dev/null || true)`. Shell word-splitting on dirs with spaces (e.g. "Visuals mining"); fixed with IFS=read loops + quoted paths + /tmp/topdirs.txt. High git porcelain (346+) from new scaffolds + deletes; handled with selective add + phase commit. No /home/workdir/.grok memory.md in env (WSL); noted in research. PII (real names/locations in memory/cloud/visuals) found in active files; replaced synthetically + documented.

5. Problem Solving: Top-level ~76/87 compliant post-batch; nested (Visuals mining/inventory subs) still partial → ongoing enforcer runs. How to do dually-targeted without duplication loops → plan for shared core + thin adapters + flags. PII in published artifacts → global synthetic rule + aux for non-code. "Do it just once" without back-forth → CQCS recurring + versioned RESEARCH/.research/ + blade law + snapshots. Test harnesses/CI + analysis of methods/false starts → dedicated phase 4.5 with full compliance. Separate web-skills vs reproducible-engine projects → informal brainstorming at end + codify process in dev env.

6. All User Messages: "ok now use firecrawl again on just the map.md portions and create a file one level above..."; "wrapper script for sure and then go back to planning stage review the analysis"; "update the same evel of the foder with a research and planning style toc"; "add on emore output to the self.esearch folde rhich explain the notes and the manifest the information you just found out and receodd to the styles and makeup tweak folder so its represented in the self.research"; "just make sure all of the research skills ae in the sma eplace top level and go look at the makeup tweak folder and the styles folder smae thing as before use firecrawl crawl to manifest the two folders and make a manifest and hten just one summary file that sets in those two folders"; "yep adn also ther eis one more folder which represent the research conducde don this in parallel by the web based grok its called web.refe