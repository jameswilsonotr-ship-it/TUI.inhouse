# AGENTS — TUI.inhouse

**Bootstrapped from:** olivia-repo-template + Olivia Dev folder-discipline.

## NEXT SESSION — READ FIRST

1. **`CURRENT-STATE.md`** (process position, deploy next, paths, archives)  
2. Then `TODO.md` / `docs/PR-ROADMAP.md` as needed  

Do **not** re-run full onboarding unless `CURRENT-STATE.md` is missing or clearly stale.

## Before structural edits

1. Read `CURRENT-STATE.md`
2. Read `FOLDER-STANDARDS.md`
3. Read `.research/dev-process-codified.md`
4. Read `STANDALONE.md` (keep package portable)
5. Check `TODO.md` and `.aux/plans/`

## After edits

```bash
python scripts/enforcer.py
```

Log phase report: `.aux/plans/<phase>-report-YYYYMMDD-HHMM.md`

## Monorepo optional

If living under grokbuild, workspace indexes may refresh via sibling `inventory-ci` — not required when standalone.
